<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			Binsta - Login
		</title>
		<link
		rel="stylesheet" href="/css/styles.css">
		<!-- Include other modern CSS libraries if needed -->
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"/>
	</head>
	<body class="font-sans bg-gray-100 text-gray-800">
		<div class="max-w-md mx-auto p-4 bg-white mb-4 border-4 my-32">
			<div class="text-center">
				<h1 class="font-light font-serif text-4xl italic">Binsta</h1>
			</div>

			<form method="post" class="mb-4">
				<div class="mb-4">
					<input type="text" id="email" name="email" placeholder="Email" class="w-full px-3 py-2 border rounded-md">
				</div>

				<div class="mb-4">
					<input type="password" id="password" name="password" placeholder="Password" class="w-full px-3 py-2 border rounded-md">
				</div>

				<div class="text-center">
					<button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-md w-full">Log in</button>
				</div>

				{% if error %}
					<div class="mt-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
						<span class="block sm:inline">{{ error }}</span>
					</div>
				{% endif %}
			</form>
		</div>

		<div class="max-w-md mx-auto p-4 bg-white mb-4 border-4">
			<div class="text-center">
				<p class="text-gray-700">Don't have an account?
					<a href="/user/register" class="text-blue-500">
						<span class="text-blue-500 font-bold">Sign up</span>
					</a>
				</p>
			</div>
		</div>
	</body>
</html>
